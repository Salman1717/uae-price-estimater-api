services:
  - type: web
    name: uae-price-estimator-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: MODEL_DIR
        value: models
